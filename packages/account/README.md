# @web-rc/biz-account

      > @web-rc/biz-account.

      ## Install

      Using npm:

      ```bash
      $ npm install --save @web-rc/biz-account
      ```

      or using yarn:

      ```bash
      $ yarn add @web-rc/biz-account
      ```
