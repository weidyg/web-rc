# @web-rc/biz-layout

      > @web-rc/biz-layout.

      ## Install

      Using npm:

      ```bash
      $ npm install --save @web-rc/biz-layout
      ```

      or using yarn:

      ```bash
      $ yarn add @web-rc/biz-layout
      ```
