# @web-rc/biz-captcha

      > @web-rc/biz-captcha.

      ## Install

      Using npm:

      ```bash
      $ npm install --save @web-rc/biz-captcha
      ```

      or using yarn:

      ```bash
      $ yarn add @web-rc/biz-captcha
      ```
