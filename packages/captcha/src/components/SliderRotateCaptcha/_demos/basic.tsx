/**
 * title: 基本使用
 * description: 基本使用
 */

import { ReactNode, useEffect, useRef, useState } from 'react';
import {
    SliderRotateCaptcha,
} from '@web-rc/biz-components';
export default () => {
    return (<>
        <SliderRotateCaptcha />
    </>);
};
