# @web-rc/biz-provider

      > @web-rc/biz-provider.

      ## Install

      Using npm:

      ```bash
      $ npm install --save @web-rc/biz-provider
      ```

      or using yarn:

      ```bash
      $ yarn add @web-rc/biz-provider
      ```
