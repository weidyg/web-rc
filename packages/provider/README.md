# @web-react/biz-provider

      > @web-react/biz-provider.

      ## Install

      Using npm:

      ```bash
      $ npm install --save @web-react/biz-provider
      ```

      or using yarn:

      ```bash
      $ yarn add @web-react/biz-provider
      ```
