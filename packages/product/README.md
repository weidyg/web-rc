# @web-rc/biz-product

      > @web-rc/biz-product.

      ## Install

      Using npm:

      ```bash
      $ npm install --save @web-rc/biz-product
      ```

      or using yarn:

      ```bash
      $ yarn add @web-rc/biz-product
      ```
