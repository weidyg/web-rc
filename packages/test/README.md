# @web-react/test

      > @web-react/test.
      
      ## Install
      
      Using npm:
      
      ```bash
      $ npm install --save @web-react/test
      ```
      
      or using yarn:
      
      ```bash
      $ yarn add @web-react/test
      ```
      